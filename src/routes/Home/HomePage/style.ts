import styled from "styled-components";

const HomeText = styled.div`
  font-size: 1.5rem;
`;

const ButtonContainer = styled.div`
  display: flex;
  justify-content: flex-end;
`;

export { HomeText, ButtonContainer };
