import styled from "styled-components";
import COLOR from "../../../constants/color";

const Title = styled.h1`
  width: max-content;
  color: ${COLOR.GRAY5};
  white-space: nowrap;
`;

export { Title };
